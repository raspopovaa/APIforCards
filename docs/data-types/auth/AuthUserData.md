# `AuthUserData`

Модель данных SDK.

!!! info "Назначение Pydantic"
    Тип модели: **response/data**. Данные проверяются вызовом `AuthUserData.model_validate(payload)`. При несовпадении типов или отсутствии обязательного поля Pydantic формирует `ValidationError`.

## Поведение модели

| Настройка | Значение | Фактическое поведение |
|---|---|---|
| Дополнительные поля (`extra`) | `allow` | Дополнительные поля разрешены и сохраняются в модели. |
| Проверка default | `True` | Значения по умолчанию также проходят валидацию. |
| Заполнение по имени поля | `True` | Разрешено использовать имя поля наряду с alias. |
| Число → строка | `True` | Для строковых полей числовые значения могут быть преобразованы в строку. |

## Поля и проверки

| Поле | Тип после валидации | JSON-тип | Обязательное | `None` | По умолчанию | Alias | Ограничения схемы | Что проверяет Pydantic | Описание |
|---|---|---|:---:|:---:|---|---|---|---|---|
| `client_id` | `str` | `string` | Да | Нет | `—` | `—` | — | Значение преобразуется и проверяется как str. | ID клиента в системе |
| `client_status` | `str` | `string` | Да | Нет | `—` | `—` | — | Значение преобразуется и проверяется как str. | Статус клиента (Active, Blocked, и т.п.) |
| `org_name` | `str \| None` | `string \| null` | Нет | Да | `None` | `—` | — | Значение должно соответствовать одному из типов: str, None | Наименование организации |
| `session_id` | `str` | `string` | Да | Нет | `—` | `—` | — | Значение преобразуется и проверяется как str. | JWT токен активной сессии |
| `user_id` | `str` | `string` | Да | Нет | `—` | `—` | — | Значение преобразуется и проверяется как str. | ID пользователя |
| `contracts` | `list[ContractInfo]` | `array[object (ContractInfo)]` | Нет | Нет | `factory: list()` | `—` | — | Проверяется как список; каждый элемент проверяется как ContractInfo. | Список доступных договоров |
| `role_id` | `str \| None` | `string \| null` | Нет | Да | `None` | `—` | — | Значение должно соответствовать одному из типов: str, None | Код роли (например, Supervisor) |
| `role_name` | `str \| None` | `string \| null` | Нет | Да | `None` | `—` | — | Значение должно соответствовать одному из типов: str, None | Название роли (например, Администратор) |
| `read_only` | `bool` | `boolean` | Нет | Нет | `False` | `—` | — | Значение преобразуется и проверяется как bool. | Флаг режима только чтение |
| `user_name` | `str \| None` | `string \| null` | Нет | Да | `None` | `—` | — | Значение должно соответствовать одному из типов: str, None | Имя пользователя |
| `user_patronymic` | `str \| None` | `string \| null` | Нет | Да | `None` | `—` | — | Значение должно соответствовать одному из типов: str, None | Отчество пользователя |
| `user_surname` | `str \| None` | `string \| null` | Нет | Да | `None` | `—` | — | Значение должно соответствовать одному из типов: str, None | Фамилия пользователя |
| `last_contract` | `str \| None` | `string \| null` | Нет | Да | `None` | `—` | — | Значение должно соответствовать одному из типов: str, None | SID последнего договора |
| `access` | `AccessRights \| None` | `object (AccessRights) \| null` | Нет | Да | `None` | `—` | — | Значение должно соответствовать одному из типов: AccessRights, None | Права доступа (web/api/mobile) |
| `email` | `str \| None` | `string \| null` | Нет | Да | `None` | `—` | — | Значение должно соответствовать одному из типов: str, None | Электронная почта |
| `phone` | `str \| None` | `string \| null` | Нет | Да | `None` | `—` | — | Значение должно соответствовать одному из типов: str, None | Телефон |

!!! note "Граница проверки"
    Значения, упомянутые только в тексте описания, не считаются жёстким ограничением. Например, фраза «Y или N» проверяется только тогда, когда в модели задан `Literal`, Enum, ограничение `Field` или пользовательский валидатор.

## Вложенные модели

- [`ContractInfo`](ContractInfo.md)
- [`AccessRights`](AccessRights.md)
