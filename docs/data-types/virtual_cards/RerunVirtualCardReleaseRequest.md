# `RerunVirtualCardReleaseRequest`

Модель данных SDK.

| Поле | Python-тип | Обязательное | Alias | Описание |
|---|---|:---:|---|---|
| `card_id` | `str` | Да | `—` | ID виртуальной карты для перезапуска выпуска |
| `reason` | `str | None` | Нет | `—` | Причина перезапуска выпуска (опционально) |
